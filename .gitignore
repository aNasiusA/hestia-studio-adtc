# Model weights — never commit these to git
model/*.gguf
model/*.bin
model/*.safetensors

# macOS
.DS_Store

# Python
__pycache__/
*.pyc
.venv/

# Local profiler output
submission.json
audit.json

# Internal working docs — production/planning artifacts, not part of the
# submission a judge should read (kept locally for reference, not in git)
video-script.md
devpost-submission-draft.md
